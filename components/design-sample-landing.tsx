"use client"

import { useEffect, useState } from "react"
import Link from "next/link"
import { ArrowRight, Check, ChevronRight, CircleHelp, Eye, ShieldCheck, Sparkles, TrendingDown, WalletCards } from "lucide-react"

const tabs = [
  { id: "forecast", label: "Forecast" },
  { id: "scenario", label: "Scenario Planner" },
  { id: "recurring", label: "Recurring" },
  { id: "transfers", label: "Transfers" },
] as const

type TabId = (typeof tabs)[number]["id"]

function CountUp({ value, prefix = "$" }: { value: number; prefix?: string }) {
  const [display, setDisplay] = useState(0)
  useEffect(() => {
    if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
      const reducedMotionFrame = requestAnimationFrame(() => setDisplay(value))
      return () => cancelAnimationFrame(reducedMotionFrame)
    }
    let frame = 0
    const start = performance.now()
    const tick = (now: number) => {
      const progress = Math.min(1, (now - start) / 850)
      setDisplay(Math.round(value * (1 - Math.pow(1 - progress, 3))))
      if (progress < 1) frame = requestAnimationFrame(tick)
    }
    frame = requestAnimationFrame(tick)
    return () => cancelAnimationFrame(frame)
  }, [value])
  return <>{prefix}{display.toLocaleString()}</>
}

const forecastPoints = [
  { x: 0, y: 56 }, { x: 9, y: 60 }, { x: 18, y: 57 }, { x: 27, y: 65 },
  { x: 36, y: 72 }, { x: 45, y: 82 }, { x: 54, y: 76 }, { x: 64, y: 84 },
  { x: 73, y: 48 }, { x: 82, y: 42 }, { x: 91, y: 38 }, { x: 100, y: 35 },
]

function linePath(points: typeof forecastPoints) {
  return points.map((point, index) => `${index === 0 ? "M" : "L"} ${point.x} ${point.y}`).join(" ")
}

function ScenarioPreview() {
  const [purchase, setPurchase] = useState(0)
  const choices = [
    { label: "No purchase", amount: 0, low: 2570, safe: 2070, condition: "Clear" },
    { label: "Weekend trip", amount: 450, low: 2120, safe: 1620, condition: "Clear" },
    { label: "Laptop", amount: 2000, low: 570, safe: 70, condition: "Tight" },
  ]
  const selected = choices[purchase]
  return <div className="space-y-4"><div className="flex items-center justify-between"><p className="text-sm font-medium text-[#152033]">Test a decision</p><span className={`rounded-full px-2.5 py-1 text-[11px] font-medium ${selected.condition === "Tight" ? "bg-[#F7E5DC] text-[#A94E2D]" : "bg-[#E2F0E9] text-[#2F7558]"}`}>{selected.condition}</span></div><div className="grid gap-2 sm:grid-cols-3">{choices.map((choice, index) => <button key={choice.label} type="button" aria-pressed={purchase === index} onClick={() => setPurchase(index)} className={`rounded-xl border px-3 py-3 text-left text-xs transition ${purchase === index ? "border-[#C9633B] bg-[#F7E5DC] text-[#152033]" : "border-[#D7E0EC] bg-white text-[#6C788C] hover:border-[#C9633B]/50"}`}><span className="block font-medium">{choice.label}</span><span className="mt-1 block font-mono">{choice.amount ? `−$${choice.amount.toLocaleString()}` : "Baseline"}</span></button>)}</div><div className="grid grid-cols-2 gap-3"><div className="rounded-xl border border-[#D7E0EC] bg-white p-3"><p className="text-[11px] text-[#6C788C]">Projected low</p><p className="mt-2 font-mono text-lg text-[#152033]">${selected.low.toLocaleString()}</p><p className="mt-1 text-[11px] text-[#6C788C]">Aug 12</p></div><div className="rounded-xl border border-[#D7E0EC] bg-white p-3"><p className="text-[11px] text-[#6C788C]">Safe to Spend</p><p className={`mt-2 font-mono text-lg ${selected.safe < 500 ? "text-[#A94E2D]" : "text-[#2F7558]"}`}>${selected.safe.toLocaleString()}</p><p className="mt-1 text-[11px] text-[#6C788C]">after $500 buffer</p></div></div><div className="rounded-xl bg-[#E8EFF8] p-3 text-sm leading-relaxed text-[#455166]">{purchase === 0 ? "Your current plan stays above the safety buffer." : selected.condition === "Tight" ? <>Buying today leaves <span className="font-medium text-[#152033]">$70 above your buffer on Aug 12.</span> Rent and the card payment arrive first.</> : <>This decision still leaves <span className="font-medium text-[#152033]">$1,620 above your buffer.</span></>}</div></div>
}

function ForecastPreview() {
  const [showWork, setShowWork] = useState(false)
  const [point, setPoint] = useState<number | null>(null)
  const selectedPoint = point === null ? null : forecastPoints[point]
  return <div className="space-y-4"><div className="flex items-center justify-between"><div><p className="text-sm font-medium text-[#152033]">Projected balance</p><p className="text-xs text-[#6C788C]">Today → Aug 25</p></div><span className="rounded-full bg-[#E2F0E9] px-2.5 py-1 text-[11px] font-medium text-[#2F7558]">Clear</span></div><div className="rounded-xl border border-[#D7E0EC] bg-white p-3"><svg viewBox="0 0 100 100" className="h-36 w-full overflow-visible" preserveAspectRatio="none" onPointerLeave={() => setPoint(null)}><line x1="0" x2="100" y1="72" y2="72" stroke="#D7E0EC" strokeDasharray="2 3" /><path d={linePath(forecastPoints)} fill="none" stroke="#C9633B" strokeWidth="2.2" pathLength="100" strokeDasharray="100" strokeDashoffset="0" />{forecastPoints.map((forecastPoint, index) => <circle key={forecastPoint.x} cx={forecastPoint.x} cy={forecastPoint.y} r={point === index ? 3.2 : 2.2} fill={point === index ? "#152033" : "#C9633B"} onPointerEnter={() => setPoint(index)} />)}<text x="66" y="96" fontSize="5" fill="#A94E2D">low point</text></svg><div className="mt-1 flex justify-between text-[10px] text-[#6C788C]"><span>Today</span><span>{selectedPoint ? `Day ${point! + 1} · $${Math.round(6200 - selectedPoint.y * 37).toLocaleString()}` : "Hover a point to inspect"}</span><span>Aug 25</span></div></div><div className="rounded-xl bg-[#E8EFF8] px-3 py-2.5"><button type="button" aria-expanded={showWork} onClick={() => setShowWork((open) => !open)} className="flex w-full items-center justify-between text-left"><span className="text-xs font-medium text-[#455166]">Safe to Spend</span><span className="font-mono text-sm font-medium text-[#2F7558]">$3,100 {showWork ? "−" : "+"}</span></button>{showWork && <div className="mt-3 space-y-2 border-t border-[#D7E0EC] pt-3 text-xs"><div className="flex justify-between text-[#6C788C]"><span>Lowest projected balance</span><span className="font-mono text-[#152033]">$3,600</span></div><div className="flex justify-between text-[#6C788C]"><span>Safety buffer</span><span className="font-mono text-[#152033]">−$500</span></div><div className="flex justify-between font-medium text-[#152033]"><span>Safe to Spend</span><span className="font-mono">$3,100</span></div></div>}</div></div>
}

function ProductPreview({ tab }: { tab: TabId }) {
  if (tab === "scenario") return <ScenarioPreview />
  if (tab === "recurring") return <div className="space-y-3"><div className="flex items-center justify-between"><p className="text-sm font-medium text-[#152033]">3 recurring patterns found</p><span className="text-xs text-[#6C788C]">Review once</span></div>{[{ name: "Rent", amount: "−$1,750", status: "confirmed", color: "#2F7558" }, { name: "Payroll", amount: "+$1,950", status: "confirmed", color: "#2F7558" }, { name: "Electricity", amount: "~−$117", status: "estimated", color: "#B7791F" }].map((item) => <div key={item.name} className="flex items-center gap-3 rounded-xl border border-[#D7E0EC] bg-white px-3 py-3"><span className="h-2 w-2 rounded-full" style={{ backgroundColor: item.color }} /><span className="flex-1"><span className="block text-sm font-medium text-[#152033]">{item.name}</span><span className="block text-[11px] capitalize text-[#6C788C]">{item.status}</span></span><span className="font-mono text-sm" style={{ color: item.color }}>{item.amount}</span></div>)}</div>
  if (tab === "transfers") return <div className="space-y-4"><div><p className="text-sm font-medium text-[#152033]">Purchases explain what you spent.</p><p className="mt-1 text-xs leading-relaxed text-[#6C788C]">The card payment is when cash leaves checking.</p></div><div className="relative space-y-2 rounded-xl border border-[#D7E0EC] bg-white p-3"><div className="flex items-center justify-between text-sm"><span className="font-medium text-[#152033]">Checking</span><span className="font-mono text-[#152033]">−$1,200</span></div><div className="mx-auto h-5 w-px border-l border-dashed border-[#C9633B]" /><div className="flex items-center justify-between text-sm"><span className="font-medium text-[#152033]">Card payment</span><span className="font-mono text-[#152033]">+$1,200</span></div><div className="mt-3 flex items-center gap-2 rounded-lg bg-[#E2F0E9] px-2.5 py-2 text-xs text-[#2F7558]"><Check className="h-3.5 w-3.5" />Linked as a transfer</div></div></div>
  return <ForecastPreview />
}

function AhaForecast() {
  const [step, setStep] = useState(0)
  const events = [{ label: "Rent", amount: "−$1,650" }, { label: "Insurance", amount: "−$180" }, { label: "Car payment", amount: "−$410" }]
  const values = ["$2,660", "$1,010", "$830", "$420"]
  return <div className="rounded-[28px] border border-[#D7E0EC] bg-white p-5 shadow-[0_18px_50px_rgba(28,28,34,0.08)] sm:p-8"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="text-xs font-medium uppercase tracking-[0.16em] text-[#C9633B]">Example forecast</p><p className="mt-1 text-sm text-[#6C788C]">Choose an event to see how it changes the balance.</p></div><span className="font-mono text-3xl font-medium text-[#152033] transition-all duration-500">{values[step]}</span></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.25fr_0.75fr] lg:items-center"><div className="rounded-2xl bg-[#EAF0F7] p-4"><svg viewBox="0 0 100 56" className="h-48 w-full overflow-visible" preserveAspectRatio="none"><line x1="0" x2="100" y1="42" y2="42" stroke="#B9C9D8" strokeDasharray="2 3" /><path d="M0 12 C15 12 19 18 31 21 S52 33 63 40 S76 44 86 28 S94 20 100 19" fill="none" stroke="#C9633B" strokeWidth="1.6" pathLength="100" strokeDasharray="100" strokeDashoffset={Math.max(0, 100 - (step + 1) * 25)} className="transition-[stroke-dashoffset] duration-700 ease-out motion-reduce:transition-none" /><circle cx={[0, 31, 52, 63][step]} cy={[12, 21, 33, 40][step]} r="2.5" fill="#C9633B" className="transition-all duration-500 motion-reduce:transition-none" /><text x="2" y="53" fontSize="4" fill="#6C788C">Today</text><text x="77" y="53" fontSize="4" fill="#6C788C">Aug 3</text></svg><div className="mt-2 flex items-center justify-between text-[11px] text-[#6C788C]"><span>Balance moves as each event arrives</span><span className="font-mono">$0 floor</span></div></div><div className="space-y-2"><button type="button" onClick={() => setStep(0)} className={`flex w-full items-center justify-between rounded-xl border px-3 py-3 text-left transition ${step === 0 ? "border-[#C9633B] bg-[#F7E5DC]" : "border-[#D7E0EC] bg-white"}`}><span className="text-sm font-medium">Starting balance</span><span className="font-mono text-sm">$2,660</span></button>{events.map((event, index) => <button type="button" onClick={() => setStep(index + 1)} key={event.label} className={`flex w-full items-center justify-between rounded-xl border px-3 py-3 text-left transition ${index < step ? "border-[#E6BDAA] bg-[#F7E5DC]" : "border-[#D7E0EC] bg-white hover:border-[#C9633B]/50"}`}><span className="flex items-center gap-2 text-sm text-[#455166]"><span className={`h-2 w-2 rounded-full ${index < step ? "bg-[#C9633B]" : "bg-[#C9D2DE]"}`} />{event.label}</span><span className="font-mono text-sm text-[#152033]">{event.amount}</span></button>)}<p className="pt-2 text-sm leading-relaxed text-[#455166]">{step === 3 ? <><span className="font-medium text-[#152033]">$420 on August 3.</span> Five days before payday.</> : "The forecast waits while you inspect each fact."}</p></div></div></div>
}

export default function DesignSampleLanding() {
  const [tab, setTab] = useState<TabId>("forecast")
  const [ahead, setAhead] = useState(false)
  const [heroReady, setHeroReady] = useState(false)
  const [scrollProgress, setScrollProgress] = useState(0)

  useEffect(() => {
    const frame = requestAnimationFrame(() => setHeroReady(true))
    const updateProgress = () => {
      const available = document.documentElement.scrollHeight - window.innerHeight
      setScrollProgress(available > 0 ? window.scrollY / available : 0)
    }
    const sections = Array.from(document.querySelectorAll<HTMLElement>("[data-sample-reveal]"))
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (!entry.isIntersecting) return
        const section = entry.target as HTMLElement
        section.dataset.visible = "true"
        observer.unobserve(section)
      })
    }, { threshold: 0.12 })
    sections.forEach((section) => observer.observe(section))
    window.addEventListener("scroll", updateProgress, { passive: true })
    updateProgress()
    return () => {
      cancelAnimationFrame(frame)
      observer.disconnect()
      window.removeEventListener("scroll", updateProgress)
    }
  }, [])

  return <main className="fs-landing min-h-screen text-[#152033] [&_[data-sample-reveal]]:translate-y-5 [&_[data-sample-reveal]]:opacity-0 [&_[data-sample-reveal]]:transition-[opacity,transform] [&_[data-sample-reveal]]:duration-700 [&_[data-sample-reveal][data-visible='true']]:translate-y-0 [&_[data-sample-reveal][data-visible='true']]:opacity-100 motion-reduce:[&_[data-sample-reveal]]:translate-y-0 motion-reduce:[&_[data-sample-reveal]]:opacity-100" style={{ background: "radial-gradient(circle at 12% 5%, rgba(255,255,255,0.98) 0%, rgba(255,255,255,0) 30%), radial-gradient(circle at 88% 13%, rgba(201,99,59,0.07) 0%, rgba(201,99,59,0) 22%), linear-gradient(180deg, #F6FAFF 0%, #EEF4FB 46%, #F2F6FC 100%)" }}>
    <div className="fixed inset-x-0 top-0 z-50 h-0.5 origin-left bg-[#C9633B]" style={{ transform: `scaleX(${scrollProgress})` }} aria-hidden="true" />
    <nav className="sticky top-0 z-20 border-b border-[#D7E0EC]/70 bg-[#F2F6FC]/90 backdrop-blur-xl"><div className="mx-auto flex h-16 max-w-6xl items-center justify-between px-5 sm:px-8"><Link href="/" className="flex items-center gap-2 text-sm font-medium"><span className="flex h-8 w-8 items-center justify-center rounded-full bg-[#152033] text-lg text-[#FFFFFF]">↗</span>FlowSight</Link><div className="hidden items-center gap-7 text-sm text-[#6C788C] md:flex"><a href="#aha">Forecast</a><a href="#features">How it works</a><a href="#security">Security</a></div><div className="flex items-center gap-3"><Link href="/sign-in" className="hidden text-sm text-[#6C788C] hover:text-[#152033] sm:inline">Sign in</Link><Link href="/sign-up" className="fs-brand-action rounded-lg px-4 py-2 text-sm font-medium">Join beta</Link></div></div></nav>

    <section className="mx-auto grid max-w-6xl gap-12 px-5 pb-20 pt-16 sm:px-8 sm:pt-24 lg:grid-cols-[0.88fr_1.12fr] lg:items-center lg:pb-28"><div className={`transition-[opacity,transform] duration-700 ${heroReady ? "translate-y-0 opacity-100" : "translate-y-4 opacity-0"}`}><p className="mb-5 text-xs font-medium uppercase tracking-[0.18em] text-[#C9633B]">Your next 30 days, explained</p><h1 className="max-w-xl text-5xl leading-[1.03] sm:text-6xl">Know what your money <span className="text-[#C9633B]">does next.</span></h1><p className="mt-6 max-w-lg text-lg text-[#455166]">FlowSight brings your balance, income, bills, and recent activity together to find the days that matter—and give you time to plan.</p><div className="mt-8 flex flex-wrap items-center gap-3"><Link href="/sign-up" className="fs-brand-action rounded-lg px-5 py-3 text-sm font-medium">Build my forecast <ArrowRight className="ml-1 inline h-4 w-4" /></Link><a href="#aha" className="rounded-lg border border-[#D7E0EC] bg-white px-5 py-3 text-sm font-medium text-[#152033] hover:border-[#C9633B]">See how it works</a></div><p className="mt-4 text-xs text-[#6C788C]">CSV or manual setup · no bank connection required</p></div><div className={`relative rounded-[28px] bg-[#EAF0F7] p-4 shadow-[0_18px_50px_rgba(28,28,34,0.08)] transition-[opacity,transform] duration-700 delay-150 hover:-translate-y-1 hover:shadow-[0_26px_65px_rgba(28,28,34,0.11)] sm:p-6 ${heroReady ? "translate-y-0 opacity-100" : "translate-y-6 opacity-0"}`}><div className="rounded-2xl border border-[#D7E0EC] bg-white p-5"><div className="flex items-start justify-between"><div><p className="text-xs text-[#6C788C]">Good morning, Soundarya</p><p className="mt-2 text-2xl font-medium">Your next 30 days</p></div><span className="rounded-full bg-[#E2F0E9] px-2.5 py-1 text-xs font-medium text-[#2F7558]">Clear</span></div><div className="mt-6 grid grid-cols-2 gap-3"><div className="rounded-xl bg-[#EAF0F7] p-3"><p className="text-xs text-[#6C788C]">Current balance</p><p className="mt-2 font-mono text-xl text-[#152033]"><CountUp value={3900} /></p></div><div className="rounded-xl bg-[#E2F0E9] p-3"><p className="text-xs text-[#6C788C]">Safe to Spend</p><p className="mt-2 font-mono text-xl text-[#2F7558]"><CountUp value={3100} /></p></div></div><div className="mt-4 rounded-xl border border-[#D7E0EC] p-3"><div className="flex items-center justify-between text-xs text-[#6C788C]"><span>Projected balance</span><span>Today → Aug 25</span></div><svg viewBox="0 0 100 42" className="mt-3 h-28 w-full" preserveAspectRatio="none"><path d="M0 12 C20 13 24 20 35 18 S50 28 62 30 S72 31 80 18 S92 16 100 14" fill="none" stroke="#C9633B" strokeWidth="1.8" pathLength="100" strokeDasharray="100" strokeDashoffset={heroReady ? 0 : 100} className="transition-[stroke-dashoffset] duration-[1400ms] ease-out motion-reduce:transition-none" /><line x1="0" x2="100" y1="32" y2="32" stroke="#D7E0EC" strokeDasharray="2 3" /></svg></div><div className={`mt-4 flex items-center gap-2 text-xs text-[#455166] transition-opacity delay-700 duration-500 ${heroReady ? "opacity-100" : "opacity-0"}`}><span className="h-2 w-2 rounded-full bg-[#C9633B]" />Rent and card payment are the next moments to watch.</div></div></div></section>

    <section data-sample-reveal id="aha" className="bg-[#E8EFF8] px-5 py-20 sm:px-8 sm:py-28"><div className="mx-auto max-w-6xl"><div className="mx-auto mb-10 max-w-2xl text-center"><p className="text-xs font-medium uppercase tracking-[0.18em] text-[#C9633B]">The moment it clicks</p><h2 className="mt-4 text-4xl leading-tight sm:text-5xl">See it <span className="text-[#C9633B]">before it arrives.</span></h2><p className="mt-4 text-lg text-[#455166]">Your projected balance drops to $420 on August 3—five days before payday. Rent, insurance, and your car payment all land in the same week.</p></div><AhaForecast /></div></section>

    <section data-sample-reveal id="features" className="px-5 py-20 sm:px-8 sm:py-28"><div className="mx-auto max-w-6xl"><div className="max-w-2xl"><p className="text-xs font-medium uppercase tracking-[0.18em] text-[#C9633B]">Inside FlowSight</p><h2 className="mt-4 text-4xl leading-tight sm:text-5xl">One picture of what’s <span className="text-[#C9633B]">ahead.</span></h2><p className="mt-4 text-lg text-[#455166]">Every view answers a different question, with the same numbers underneath.</p></div><div className="mt-10 grid gap-8 lg:grid-cols-[0.7fr_1.3fr] lg:items-start"><div className="space-y-2">{tabs.map((item) => <button key={item.id} type="button" onClick={() => setTab(item.id)} className={`flex w-full items-center justify-between rounded-xl px-4 py-4 text-left text-sm transition ${tab === item.id ? "bg-[#152033] text-white" : "text-[#455166] hover:bg-[#EAF0F7]"}`}><span>{item.label}</span><ChevronRight className={`h-4 w-4 transition-transform ${tab === item.id ? "translate-x-1" : ""}`} /></button>)}</div><div className="rounded-[28px] bg-[#EAF0F7] p-4 sm:p-7"><ProductPreview tab={tab} /></div></div></div></section>

    <section data-sample-reveal className="border-y border-[#D7E0EC] bg-[#EAF0F7] px-5 py-20 sm:px-8 sm:py-28"><div className="mx-auto max-w-6xl"><div className="mx-auto max-w-2xl text-center"><p className="text-xs font-medium uppercase tracking-[0.18em] text-[#C9633B]">Built for real life</p><h2 className="mt-4 text-4xl leading-tight sm:text-5xl">Know what FlowSight <span className="text-[#C9633B]">knows.</span></h2></div><div className="mt-12 grid gap-4 md:grid-cols-2"><div className="fs-interactive rounded-2xl border border-[#D7E0EC] bg-white p-6"><WalletCards className="h-5 w-5 text-[#C9633B]" /><h3 className="mt-5 text-xl font-medium">Confirmed vs. estimated</h3><p className="mt-2 text-sm text-[#6C788C]">Stable rent and variable electricity never look like the same kind of fact.</p><div className="mt-6 flex gap-2"><span className="rounded-full bg-[#E2F0E9] px-3 py-1.5 text-xs text-[#2F7558]">Confirmed</span><span className="rounded-full bg-[#FFF5D9] px-3 py-1.5 text-xs text-[#B7791F]">Estimated</span></div></div><div className="fs-interactive rounded-2xl border border-[#D7E0EC] bg-white p-6"><TrendingDown className="h-5 w-5 text-[#C9633B]" /><h3 className="mt-5 text-xl font-medium">Timing, not just totals</h3><p className="mt-2 text-sm text-[#6C788C]">See the week a paycheck, rent, and a card payment all meet.</p><div className="mt-6 flex items-end gap-2" aria-label="Income timing illustration">{[18, 30, 18, 42, 18, 26, 18].map((height, index) => <span key={index} className={`w-5 rounded-t ${index === 3 ? "bg-[#C9633B]" : "bg-[#D7E0EC]"}`} style={{ height }} />)}</div></div><div className="fs-interactive rounded-2xl border border-[#D7E0EC] bg-white p-6"><Eye className="h-5 w-5 text-[#C9633B]" /><h3 className="mt-5 text-xl font-medium">Show your work</h3><p className="mt-2 text-sm text-[#6C788C]">Tap any result and see the balance, activity, buffer, and assumptions behind it.</p><div className="mt-6 rounded-xl bg-[#EAF0F7] p-3 text-xs text-[#455166]">$3,840 balance + $420 activity = <span className="font-mono font-medium text-[#152033]">$4,260 opening</span></div></div><div className="fs-interactive rounded-2xl border border-[#D7E0EC] bg-white p-6"><Sparkles className="h-5 w-5 text-[#C9633B]" /><h3 className="mt-5 text-xl font-medium">Income that doesn’t fit one mold</h3><p className="mt-2 text-sm text-[#6C788C]">Regular paychecks, freelance invoices, or both—each pattern gets its own honest range.</p><div className="mt-6 flex items-end gap-2">{[24, 38, 21, 46, 29, 54].map((height, index) => <span key={index} className={`w-5 rounded-t ${index % 2 === 0 ? "bg-[#2F7558]" : "bg-[#C9633B]"}`} style={{ height }} />)}</div></div></div></div></section>

    <section data-sample-reveal className="px-5 py-20 sm:px-8 sm:py-28"><div className="mx-auto grid max-w-6xl gap-10 lg:grid-cols-2 lg:items-center"><div><p className="text-xs font-medium uppercase tracking-[0.18em] text-[#C9633B]">A clearer question</p><h2 className="mt-4 text-4xl leading-tight sm:text-5xl">Stop looking backward. <span className="text-[#C9633B]">Start planning forward.</span></h2><p className="mt-5 max-w-lg text-lg text-[#455166]">Most tools organize what already happened. FlowSight shows what the next few weeks may hold—and what to do if money gets tight.</p></div><div className="rounded-[28px] bg-[#152033] p-5 text-white sm:p-7"><div className="flex gap-2 rounded-xl bg-white/10 p-1 text-sm"><button type="button" onClick={() => setAhead(false)} className={`flex-1 rounded-lg px-3 py-2 ${!ahead ? "bg-white text-[#152033]" : "text-white/65"}`}>Looking back</button><button type="button" onClick={() => setAhead(true)} className={`flex-1 rounded-lg px-3 py-2 ${ahead ? "bg-white text-[#152033]" : "text-white/65"}`}>Looking ahead</button></div><div className="mt-8"><p className="text-sm text-white/60">{ahead ? "What FlowSight tells you" : "What you’re used to"}</p><div className="mt-4 space-y-3">{(ahead ? ["You have $820 safe to spend until the 18th", "Rent and insurance create a low point on August 3", "A late invoice changes the plan—not your history"] : ["You spent $200 on dining last month", "An alert arrived after the overdraft", "A fixed budget ignored your actual timing"]).map((item) => <div key={item} className={`flex items-start gap-3 rounded-xl border p-3 text-sm ${ahead ? "border-[#2F7558]/35 bg-[#2F7558]/10" : "border-white/10 bg-white/5 text-white/75"}`}><span className={`mt-0.5 flex h-5 w-5 shrink-0 items-center justify-center rounded-full ${ahead ? "bg-[#2F7558]" : "bg-white/10"}`}>{ahead ? <Check className="h-3.5 w-3.5" /> : "–"}</span>{item}</div>)}</div></div></div></div></section>

    <section data-sample-reveal id="security" className="bg-[#E8EFF8] px-5 py-20 sm:px-8 sm:py-28"><div className="mx-auto max-w-6xl"><div className="mx-auto max-w-2xl text-center"><p className="text-xs font-medium uppercase tracking-[0.18em] text-[#C9633B]">Private by design</p><h2 className="mt-4 text-4xl leading-tight sm:text-5xl">Start with your numbers, <span className="text-[#C9633B]">stay in control.</span></h2></div><div className="mt-10 grid gap-4 md:grid-cols-3">{[{ icon: ShieldCheck, title: "No bank connection required", copy: "Import a CSV or add details manually. Your login never needs to leave your bank." }, { icon: CircleHelp, title: "Every number has a reason", copy: "Confirmed events, estimates, ranges, and assumptions stay visible." }, { icon: WalletCards, title: "Nothing changes without you", copy: "Scenarios, transfers, and recurring patterns wait for your confirmation." }].map(({ icon: Icon, title, copy }) => <div key={title} className="rounded-2xl border border-[#D7E0EC] bg-white p-6"><Icon className="h-5 w-5 text-[#C9633B]" /><h3 className="mt-5 text-lg font-medium">{title}</h3><p className="mt-2 text-sm leading-relaxed text-[#6C788C]">{copy}</p></div>)}</div></div></section>

    <section data-sample-reveal className="px-5 py-20 sm:px-8 sm:py-28"><div className="mx-auto max-w-4xl rounded-[32px] bg-[#152033] px-6 py-16 text-center text-white sm:px-12"><p className="text-xs font-medium uppercase tracking-[0.18em] text-[#E2A080]">See what’s next</p><h2 className="mt-4 text-4xl leading-tight sm:text-5xl">Stop guessing. <span className="text-[#E2A080]">Start knowing.</span></h2><p className="mx-auto mt-5 max-w-xl text-base leading-relaxed text-white/70">Bring your numbers. FlowSight will show you the days that matter before they arrive.</p><Link href="/sign-up" className="mt-8 inline-flex items-center rounded-lg bg-[#F2F6FC] px-5 py-3 text-sm font-medium text-[#152033] hover:bg-[#E8EFF8]">Join the beta <ArrowRight className="ml-2 h-4 w-4" /></Link></div></section>
    <footer className="border-t border-[#D7E0EC] px-5 py-8 text-xs text-[#6C788C] sm:px-8"><div className="mx-auto flex max-w-6xl flex-wrap items-center justify-between gap-3"><span className="font-medium text-[#152033]">FlowSight</span><span>Forward-looking cash-flow clarity.</span><span>Design sample · not the live landing page</span></div></footer>
  </main>
}
